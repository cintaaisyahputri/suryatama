<?php

/*
|--------------------------------------------------------------------------
| CONTOH TAMBAHAN UNTUK app/Models/User.php BAWAAN LARAVEL
|--------------------------------------------------------------------------
| File ini BUKAN untuk ditimpa langsung — App\Models\User bawaan Laravel
| sudah berisi setup password/auth yang penting. Cukup salin bagian-bagian
| di bawah ini ke dalam User.php milikmu.
*/

// 1. Tambahkan ke $fillable:
protected $fillable = [
    'name',
    'email',
    'password',
    'role',              // 'user' atau 'admin'
    'city',
    'address',
    'latitude',
    'longitude',
    'has_seen_tutorial',
];

// 2. Tambahkan cast (opsional, biar has_seen_tutorial jadi boolean asli):
protected $casts = [
    'email_verified_at' => 'datetime',
    'password' => 'hashed',
    'has_seen_tutorial' => 'boolean',
];

// 3. Tambahkan relasi ke Order:
public function orders(): \Illuminate\Database\Eloquent\Relations\HasMany
{
    return $this->hasMany(\App\Models\Order::class);
}
